{
  "name": "timetoprod",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "npm run dev --workspace=packages/timetoprod-server",
    "dev:web": "npm run dev --workspace=packages/timetoprod-web",
    "build": "npm run build --workspaces",
    "test": "npm run test --workspace=packages/timetoprod-server",
    "lint": "npm run lint --workspaces"
  },
  "engines": {
    "node": ">=22"
  }
}
