{
  "name": "takt",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "npm run dev --workspace=packages/takt-server",
    "dev:web": "npm run dev --workspace=packages/takt-web",
    "build": "npm run build --workspaces",
    "test": "npm run test --workspace=packages/takt-server",
    "lint": "npm run lint --workspaces"
  },
  "engines": {
    "node": ">=22"
  }
}
